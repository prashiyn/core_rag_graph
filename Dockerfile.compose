FROM python:3.11-slim

ENV PYTHONDONTWRITEBYTECODE=1 \
    PYTHONUNBUFFERED=1

WORKDIR /app

RUN pip install --no-cache-dir uv

COPY . .

RUN uv sync

EXPOSE 20050

CMD ["uv", "run", "uvicorn", "graph_server:app", "--host", "0.0.0.0", "--port", "20050"]
